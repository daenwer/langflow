1. Создать файл `.env` (для демонстрации достаточно скопировать из `.env.example`).
   Флаг `LANGFLOW_BACKEND_ONLY=false/true` включает или выключает UI.

2. Сервис доступен на `http://<host>:7860` (`/flow` — UI, `/docs` — Swagger).
